- TOML 配置按功能域分节（[llm]/[embedding]/[knowledge]/[storage]/[export]），每个 provider 用子节区分（如 [llm.ollama]、[embedding.openai]）。
- 敏感配置文件 `settings.toml` 加入 `.gitignore`，仅提供 `settings.example.toml` 作为可提交模板。
- 数据目录采用空 `.gitkeep` 占位，真实产物（JSON、vector_db、exports）统一由运行期写入并被 git 忽略。